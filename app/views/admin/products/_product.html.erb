<tr>                                                                                             
  <th> 
    <%= product.sku %> 
  </th>
  <th><%= product.title %></th>
  <th><%= product.desc %></th>
  <th>
    <% if product.thumb_image.thumb.present? %>
      <%= image_tag product.thumb_image.thumb.url %>
  <% end %>
</th>
  <th><%= link_to "编辑", [:edit, :admin, product], class: "btn btn-primary" %></th>
</tr>                                                                                            

<tr>                                                                                             
  <th> 
    <%= product.sku %> 
  </th>
  <th><%= product.title %></th>
  <th><%= product.desc %></th>
  <th>
    <% if product.thumb_image.thumb.present? %>
      <%= image_tag product.thumb_image.thumb.url %>
  <% end %>
</th>
  <th><%= link_to "编辑", [:edit, :admin, product], class: "btn btn-primary" %> <%= link_to "删除", [:admin, product], method: :delete, class: "btn btn-primary", data: { confirm: "确认删除吗?" } %></th>
</tr>                                                                                            

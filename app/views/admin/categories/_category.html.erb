<tr>                                                                                             
  <th> 
    <%= category.name %> 
  </th>
  <th><%= category.parent_id.present? ? category.parent.try(:name) : "" %></th>
  <th><%= category.note %></th>
  <th><%= link_to "编辑", [:edit, :admin, category], class: "btn btn-primary" %></th>
</tr>                                                                                            

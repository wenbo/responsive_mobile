<!DOCTYPE html>
<html class="bg-black">
<head>
  <meta charset="UTF-8">
  <title>CMS系统登陆 | HIOKI</title>
  <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
  <%= stylesheet_link_tag 'login', media: 'all' %>
   <%= javascript_include_tag "application", :media=>"all"%>
  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
  <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  <![endif]-->
  <%= csrf_meta_tags %>
</head>
<body class="bg-black">
<div class="form-box" id="login-box">
  <div class="header">HIOKI 管理系统登录</div>
  <%= form_tag [:admin, :login] do %>
      <div class="body bg-gray">
        <% if flash[:notice].present? %>
          <div class="form-group" style="color: red;">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <div class="form-group">
          <%= text_field_tag "username", params[:username], class: "form-control", placeholder: "用户名" %>
        </div>
        <div class="form-group">
          <input type="password" name="password" class="form-control" placeholder="密码"/>
        </div>
         <%= show_simple_captcha %>
        <div class="form-group">
          <label style="font-weight: normal;"> 
            <input type="checkbox" name="remember_me"/> 记住我 
          </label>
        </div>
      </div>
      <div class="footer">
        <button type="submit" class="btn bg-hioki btn-block">提交</button>
      </div>
  <% end %>
  <div class="margin text-center"></div>
</div>
</body>
</html>
<script>
    // $(function(){
    //     var captcha = $("img[alt='captcha']");
    //     var captcha_url = captcha.attr('src');
    //     var captcha_src = "/captcha"+captcha_url.split("/captcha")[1]
    //     captcha.attr("src",captcha_src);

    //     $("img[alt='captcha']").each(function(index, item){
    //         item.title = '看不清？点击刷新';
    //     });
    //     $("img[alt='captcha']").bind("click", function(){
    //         var src_now = "/captcha"+this.src.split("/captcha")[1]
    //         this.src = src_now + '?' ;
    //     });
    // });
</script>

<tr>                                                                                             
  <th> 
    <%= faq.id %> 
  </th>
  <th><%= faq.name %></th>
	<th><%= faq.faq_category.name %></th>
  <th><%= parse_date(faq.public_time) %></th>
  <th><%= link_to "编辑", [:edit, :admin, faq], class: "btn btn-primary" %> <%= link_to "删除", [:admin, faq], method: :delete, class: "btn btn-primary", data: { confirm: "确认删除吗?" } %></th>
</tr>                                                                                            
